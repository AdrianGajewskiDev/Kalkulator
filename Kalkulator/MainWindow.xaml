<Window x:Class="Kalkulator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Kalkulator"
        mc:Ignorable="d"
        Title="MainWindow" 
        Height="450"
        Width="800"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        >
    <Border>
        <Grid>
            <!--Row for close, minimize and maximize buttons-->
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!--Grid for buttons-->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="500"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid.Triggers>
                    <EventTrigger RoutedEvent="MouseEnter">
                        <BeginStoryboard>
                            <Storyboard>
                                <ColorAnimation To="Red" Duration="0:0:0.5" 
                                                Storyboard.TargetName="CloseButton"
                                                Storyboard.TargetProperty="Background.Color"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                    <EventTrigger RoutedEvent="MouseLeave">
                        <BeginStoryboard>
                            <Storyboard>
                                <ColorAnimation To="Gray" Duration="0:0:0.5" 
                                                Storyboard.TargetName="CloseButton"
                                                Storyboard.TargetProperty="Background.Color"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Grid.Triggers>
                    
                <!--Title-->
                <Grid Background="Red">
                    <TextBlock Text="{Binding Title,FallbackValue=Kalkulator}"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Right"
                           Margin="0 0 40 0"
                           FontSize="30"/>
                </Grid>

                <StackPanel Grid.Column="1"
                        Orientation="Horizontal">
                    <Button Content="_" Width="100" Command="{Binding WindowMinimizeCommand}"/>
                    <Button Content="[ ]" Width="100" Command="{Binding WindowMaximizeCommand}"/>
                    <Button Content="X" Width="100" x:Name="CloseButton" Command="{Binding WindowCloseCommand}"/>
                </StackPanel>

            </Grid>
        </Grid>
    </Border>

</Window>
